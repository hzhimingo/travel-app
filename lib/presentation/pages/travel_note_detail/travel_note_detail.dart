import 'package:flutter/material.dart';

class TravelNoteDetail extends StatelessWidget {
  const TravelNoteDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}