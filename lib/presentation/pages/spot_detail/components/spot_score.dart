import 'package:flutter/material.dart';

class SpotScore extends StatelessWidget {
  const SpotScore({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}