import 'package:flutter/material.dart';

class ProfileQuestionTabView extends StatelessWidget {
  const ProfileQuestionTabView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('QuestionTabView'),
      ),
    );
  }
}