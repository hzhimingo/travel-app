import 'package:flutter/material.dart';

class ProfileVideoTabView extends StatelessWidget {
  const ProfileVideoTabView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('这里空空如也'),
      ),
    );
  }
}