import 'package:flutter/material.dart';

class ProfileTravelNoteTabView extends StatelessWidget {
  const ProfileTravelNoteTabView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('TravelNoteTabView'),
      ),
    );
  }
}