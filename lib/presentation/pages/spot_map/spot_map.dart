
import 'package:flutter/material.dart';

class SpotMap extends StatelessWidget {
  const SpotMap({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Spot List'),
      ),
    );
  }
}
