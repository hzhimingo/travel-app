import 'package:flutter/material.dart';

class UserInfoTitle extends StatelessWidget {
  const UserInfoTitle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}